#ifndef GUARD_change_h
#define GUARD_change_h

#include <string>
#include <vector>


bool space(char);
bool not_space(char);
std::vector<std::string> split(const std::string&);

#endif // !GUARD_frame_h
